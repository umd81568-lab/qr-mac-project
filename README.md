# qr-mac-project

Simple dashboard app for:
- creating custom `domain + path` live links
- attaching a PDF file to each link
- auto-generating and saving QR code for each link
- tracking minimal usage data (access count + last access time)

## Run

```bash
pip install -r requirements.txt
python app.py
```

Open `http://127.0.0.1:5000`.